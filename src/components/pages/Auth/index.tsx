import React from 'react';
import styles from "./Auth.module.scss";
import classNames from 'classnames';

const Auth = () => {
    return (
        <div>

        </div>
    );
};

export default Auth;